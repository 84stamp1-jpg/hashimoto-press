# ARCHITECTURE.md — HHub 全体構成とデータフロー

最終更新: 2026-07-04(チャット側の記録を基に作成。実リポジトリと照合のうえ随時更新すること)

## 全体像

```
┌─────────────────────── 社外(クラウド) ───────────────────────┐
│                                                               │
│  GitHub Pages ──────── HHub(ポータル)+ 各システムHTML         │
│      │                                                        │
│  Firebase RTDB ─────── リアルタイムデータ(位置・進捗・状態)      │
│  (asia-southeast1)                                            │
│      │                                                        │
│  kintone ───────────── 業務マスタ(金型・品番・保全・プロセス)     │
│  (hashimoto-kogyo)                                            │
│      │                                                        │
│  GAS ───────────────── kintoneプロキシ(JSONP)/PDF生成/Sheets   │
│      │                                                        │
│  Slack ─────────────── 通知・リマインダー・月次会レポート投稿      │
│                                                               │
└───────────────────────────────────────────────────────────────┘
                              ▲
                              │ HTTPS / WebSocket
┌─────────────────────── 社内(工場LAN) ─────────────────────────┐
│                                                               │
│  現場PC・タブレット ──── ブラウザでHHubを表示                     │
│  常時稼働PC ─────────── BLEビーコンスキャナー(Node.js)           │
│                         → Firebaseへ位置データ送信               │
│                         ※カメラ中継サーバー(go2rtc)の設置候補    │
│  ESP32群 ────────────── ショットカウンター(PC817C)               │
│                         → Firebaseへショット数送信               │
│  BLEビーコン ─────────── MM-BLEBC5(作業者・金型に取付)           │
│  Tapoカメラ(予定) ────── RTSP配信 → 社内中継サーバー経由でHHubへ  │
│                                                               │
└───────────────────────────────────────────────────────────────┘
```

## データフローの原則

1. **リアルタイム性が必要なもの** → Firebase RTDB
   (ビーコン位置、プレス進捗、修繕計画のタスク、トラブルボタン状態)
2. **業務マスタ・履歴** → kintone
   (品番情報、金型情報、保全記録、プロセス管理)
3. **ブラウザ→kintone のCORS回避** → GASプロキシ(JSONP)。
   kintoneポータル埋め込み時は `kintone.api()` 直接呼び出しに切替
4. **定期処理・通知** → GAS(トリガー)または常時稼働PCのスクリプト → Slack

## Firebase データパス一覧(把握分)

| パス | 用途 | 書き込み元 |
|------|------|-----------|
| `/beacons/...` | BLEビーコン位置 | ビーコンスキャナーPC |
| `/moldRepairPlan/tasks` | 金型修繕タスク | mold_repair_plan.html |
| (進捗系パス) | プレス進捗・入力履歴 | press_dashboard_v11.html |
| (ショット系パス) | プレス機ショット数 | ESP32 |
| `/trouble/...`(予定) | トラブルボタン状態 | ESP32ボタン(カメラシステム) |
| `/cameras/...`(予定) | カメラ設定・タイムラプス索引 | カメラ中継サーバー |

※正確なパス名は各HTMLの実装を確認して本表を更新すること。

## kintone アプリ一覧(把握分)

| App ID | 用途 | 連携先 |
|--------|------|--------|
| 6 | 金型保全・修繕 | mold_repair_plan.html、保全スケジュール |
| 7 | 段取りナビ(品番マスタ) | dantori_navi.html |
| 75 | プロセス管理 | 基準遵守手当システム(連携予定) |

## 社内LANのみで完結する系(クラウド非経由)

- Tapoカメラの映像ストリーム(RTSP → go2rtc → ブラウザ)。
  映像そのものは社外に出さない。HHubページは映像を社内サーバーから直接受ける
